OCMP43.xlsx includes the coded segments, filterable by platform, topic, and code. 

Text/ includes the documents resulting from the scraping and processing prior to annotation. 
The original source URLs as well as non-coded text (e.g., duplicate text or irrelevant text from false positive keyword hits) are also included in Text/.